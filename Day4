Question No.1 - a. Print odd numbers in an array

let count = [0,2,3,5,4,9,8,1,3,7];

(() =>{
for (let i = 0; i<count.length; i++)
{
if(count[i]%2 !=0)
{
console.log(count[i]);
}
}
})();



c.Sum of all numbers in an array

let array = [1, 3, 6, 8, 6, 7]

function addArray(array)
{
	let sum = 0

	for (let i=0; i<array.length; i+=1)
{
	sum += array[i]
}
  return sum;
}

console.log(addArray([1, 3, 6, 8, 6, 7]));



